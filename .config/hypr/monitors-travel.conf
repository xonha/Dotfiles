# Travel profile (laptop + one portable external monitor).
# TODO: configure once the portable monitor description is known.
# Run `hyprctl monitors all` while the monitor is connected to get its description,
# then add entries like:
#   monitor=desc:BOE 0x06CF,preferred,auto,1
#   monitor=desc:<portable monitor description>,preferred,auto,1
